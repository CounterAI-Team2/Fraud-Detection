options(stringsAsFactors = FALSE)

counterai_config <- list(
  seed = 147,
  db_path = "counterai_mvp.sqlite",
  model_artifact = "output/model_bundle.rds",
  str_template = "templates/str_template.md"
)
